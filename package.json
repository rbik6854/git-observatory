{
  "name": "interactive-git-learning",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build:packages": "tsc -b packages/core-domain packages/core-analysis packages/core-lessons packages/ui-shared packages/desktop-git-adapter",
    "build": "npm run build:packages && npm run build -w @git-observatory/desktop-shell",
    "test": "vitest run",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@types/node": "^24.6.0",
    "playwright": "^1.55.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
