# Claude Code Adapter

This directory is a thin adapter over the canonical agent system.

- Canonical skills live in `docs/agents/skills/`.
- Canonical contracts live in `.agents/contracts/`.
- Do not fork skill behavior here.

Use `.claude/skills-index.json` as the entrypoint for Claude Code-specific wiring.
