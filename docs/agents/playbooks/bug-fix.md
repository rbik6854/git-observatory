# Bug Fix Playbook

1. Capture a repro in the task context.
2. Assign `researcher` or `tester` first if root cause is unclear.
3. Keep the fix scoped to the owned paths.
4. Add targeted verification beyond general tests.
5. Record the root cause in the handoff.
