# VS Code Agent Guidance

This workspace uses the canonical agent system in `docs/agents/` and `.agents/`.

- Skills are defined once in `docs/agents/skills/`.
- VS Code files in this directory only provide editor integration.
- Task packets and validation rules come from the canonical contracts, not from VS Code-specific copies.
